"Main - Paper Figures" includes all the code use to generate paper figures from SHAP explanation objects

"Pickle SHAP datasets example" includes an example of how the pickled SHAP explanation objects were created form the raw data

To run the code, download the "environment.yml" file. The raw data should also be downloaded from Zenodo (doi: 10.5281/zenodo.18495402)
